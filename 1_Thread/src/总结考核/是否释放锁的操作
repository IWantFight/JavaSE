释放锁的操作：
    同步方法、同步代码块结束
    同步方法、同步代码块遇到break、return
    同步方法、同步代码块遇到未处理的Error或者Exception
    当前线程在同步方法、同步代码块中执行了wait操作，当前线程暂停并释放锁。
不会释放锁的操作：
    当前线程在同步方法、同步代码块中调用了sleep、yeild等操作
    其他线程调用了当前线程的suspend操作将改线程挂起。